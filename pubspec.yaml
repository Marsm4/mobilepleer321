name: musicplayer52
description: "A new Flutter project."
publish_to: 'none' # Этот параметр отключает публикацию в pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.15
  supabase_flutter: ^1.1.1
  audioplayers: ^6.4.0
  image_picker: ^1.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


  assets:
    - images/icon.png

